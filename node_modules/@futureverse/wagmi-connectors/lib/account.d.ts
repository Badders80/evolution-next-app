import { FutureverseAuthClient, SignInOptions } from '@futureverse/auth';
import { Signer } from '@futureverse/signer';
export declare function signInFuturePass(authClient: FutureverseAuthClient, opts: {
    authType: string;
    signer?: Signer;
    authName?: string;
    connectorId: string;
    signOpts?: SignInOptions;
}): Promise<void>;
