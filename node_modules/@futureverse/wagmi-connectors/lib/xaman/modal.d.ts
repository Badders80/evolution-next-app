import { EventEmitter } from 'events';
import { XummPostPayloadResponse } from '@futureverse/signer';
export declare class QrModal extends EventEmitter {
    modal: HTMLDivElement | null;
    imageSrc: string;
    headerText: string;
    bodyText: string;
    uuid: string;
    constructor({ payload, headerText, bodyText, }: {
        payload: XummPostPayloadResponse;
        headerText?: string;
        bodyText?: string;
    });
    createModal(): void;
    private makeDraggable;
    cancelAndDestroy(): void;
    destroy(): void;
}
