import { Connector } from 'wagmi';
/** Action to convert a Viem Client to a Futureverse Signer. */
export declare function useFutureverseSigner({ chainId, connector, }?: {
    chainId?: number;
    connector?: Connector;
}): import('@futureverse/signer').Signer | undefined;
