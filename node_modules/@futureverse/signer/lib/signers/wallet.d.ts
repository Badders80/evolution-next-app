import { Account, Chain, Client, Hex, RpcSchema, SignableMessage, Transport } from 'viem';
import { Signer } from './signer';
export type WalletSignerClient = Client<Transport, Chain, Account, RpcSchema, {
    signMessage?: (params: {
        message: SignableMessage;
    }) => Promise<Hex>;
}>;
export declare class WalletSigner extends Signer {
    private _walletClient;
    constructor(walletClient: WalletSignerClient);
    getAddress(): Promise<`0x${string}`>;
    signMessage(message: SignableMessage): Promise<`0x${string}`>;
}
