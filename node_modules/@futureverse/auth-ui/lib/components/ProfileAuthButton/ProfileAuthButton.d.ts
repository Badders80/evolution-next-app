import { default as React } from 'react';
import { IconName } from '../IconFactory/IconFactory';
import { AvatarSize, AvatarType } from '../Avatar/Avatar';
type ProfileAuthButtonProps = React.ButtonHTMLAttributes<HTMLButtonElement> & {
    avatarType?: AvatarType;
    src?: string;
    username: string;
    brand?: string;
    iconName?: IconName;
    iconSize?: AvatarSize;
};
export declare const ProfileAuthButton: React.FC<ProfileAuthButtonProps>;
export default ProfileAuthButton;
