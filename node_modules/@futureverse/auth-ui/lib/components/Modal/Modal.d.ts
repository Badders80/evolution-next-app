import { PropsWithChildren } from 'react';
import { State } from '../../providers';
import { ThemeConfig } from '../../themes/DefaultThemes';
import { CustodialOptions } from '../CustodialAuthOptions/CustodialAuthOptions';
import { Web3Options } from '../Web3AuthOptions/Web3AuthOptions';
export type ModalProps = {
    themeConfig: ThemeConfig;
    show?: boolean;
    currentState?: State;
    currentConnector?: string;
    error?: Error | null;
    onClose?: () => void;
    onBack?: () => void;
    onRetry?: () => void;
    connectAndSignIn: (connectorId: string) => Promise<void>;
    onSignPass: () => Promise<void>;
    custodialAuthOptions: CustodialOptions[];
    web3AuthOptions: Web3Options[];
};
export declare function Modal({ themeConfig, currentState, currentConnector, show, error, onBack, onClose, connectAndSignIn, onSignPass, onRetry, web3AuthOptions, custodialAuthOptions, children, }: PropsWithChildren<ModalProps>): string | number | boolean | Iterable<import('react').ReactNode> | import("react/jsx-runtime").JSX.Element | null | undefined;
